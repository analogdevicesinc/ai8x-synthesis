---
arch: test
dataset: test_burnin_64x32x32
weights: weights_test_burnin-alternating

# Define layer parameters in order of the layer sequence
layers:
  - pad: 1
    activate: ReLU
    out_offset: 0x8000
    processors: 0x000000000000ffff
    data_format: HWC
    op: Conv2d
