# Benchmark network. Uses all processors.

arch: ai85net5
dataset: conv2dTranspose

# Define layer parameters in order of the layer sequence
layers:
- pad: 1
  processors: 0x0000000000000001
  #data_format: CHW
  out_offset: 0x2000
  op: ConvTranspose2d
  kernel_size: 3x3
  stride: 2
